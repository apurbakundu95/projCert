---
- hosts: all
  become: true

  tasks:
    - name: Install Docker dependencies
      apt:
        name: 
          - docker.io
        state: present
        update_cache: yes

    - name: Start Docker service
      service:
        name: docker
        state: started
        enabled: yes

